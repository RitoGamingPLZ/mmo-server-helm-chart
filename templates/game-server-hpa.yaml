{{- range .Values.server.zones }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $.Values.server.autoscaler.name }}-{{ .id }}
  namespace: {{ $.Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $.Values.server.deployment.name }}-{{ .id }}
  minReplicas: {{ $.Values.server.autoscaler.minReplicas }}
  maxReplicas: {{ $.Values.server.autoscaler.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80 # scale up if avg CPU > 60%
{{- end }}
